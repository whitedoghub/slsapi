{
  "name": "slsapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start:dev": "rm -rf ./build && tsc && NODE_ENV=dev nodemon --watch src --exec ts-node ./src/app.ts",
    "start:staging": "rm -rf ./build && tsc && NODE_ENV=staging nodemon --watch src --exec ts-node ./src/app.ts",
    "start": "tsc && node ./build/src/app.js",
    "build": "tsc"
  },
  "repository": "https://github.com/whitedoghub/slsapi.git",
  "author": "whitedoghub <kbk109@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/koa": "^2.13.1",
    "@types/koa-route": "^3.2.4",
    "@types/node": "^15.0.1",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "koa": "^2.13.1",
    "koa-router": "^10.0.0"
  }
}
